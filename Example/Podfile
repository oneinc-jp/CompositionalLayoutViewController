use_frameworks!

platform :ios, '9.0'

target 'CompositionalLayoutViewController_Example' do
  pod 'CompositionalLayoutViewController', :path => '../'

  target 'CompositionalLayoutViewController_Tests' do
    inherit! :search_paths

    
  end
end
